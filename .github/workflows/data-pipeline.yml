name: Data Pipeline

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * 1"

concurrency:
  group: data-pipeline-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          # cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run pipeline
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: |
          python src/run_analysis_pipeline.py

#      - name: Execute notebook
#        env:
#          PYTHONPATH: ${{ github.workspace }}
#        run: |
#          jupyter nbconvert --to notebook --execute --inplace notebooks/03_coverage_gap_analysis.ipynb

#      - name: Build notebook HTML
#        run: |
#          mkdir -p site
#          jupyter nbconvert --to html notebooks/02_coverage_gap_analysis.ipynb --output index --output-dir site
#          touch site/.nojekyll
#
#      - name: Upload workflow artifacts
#        uses: actions/upload-artifact@v4
#        with:
#          name: coverage-analysis-output
#          path: |
#            data/processed/**
#            notebooks/03_coverage_gap_analysis.ipynb
#            site/**
#          retention-days: 14
#
#      - name: Upload Pages artifact
#        uses: actions/upload-pages-artifact@v4
#        with:
#          path: site
#
#  deploy-pages:
#    needs: build-data
#    runs-on: ubuntu-latest
#
#    permissions:
#      contents: read
#      pages: write
#      id-token: write
#
#    environment:
#      name: github-pages
#      url: ${{ steps.deployment.outputs.page_url }}
#
#    steps:
#      - name: Deploy to GitHub Pages
#        id: deployment
#        uses: actions/deploy-pages@v4